# Core dependencies — version ranges tested on Python 3.10–3.14
sentence-transformers>=2.7.0
numpy>=1.26.0
pandas>=2.0.0
pyarrow>=14.0.0
tqdm>=4.66.0
xgboost>=2.0.0
pytest>=8.0.0

# torch is intentionally excluded.
# Install it separately to match your environment:
#
#   CPU only:
#     pip install torch
#
#   CUDA (local GPU):
# pip install torch --index-url https://download.pytorch.org/whl/cu121
#
#   Google Colab:
#     Already pre-installed — do NOT reinstall (breaks CUDA build).
#
# sentence-transformers will use whatever torch is already present.
